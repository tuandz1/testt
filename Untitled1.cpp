#include <stdio.h>

int main () {
/*int a,b,c;
   scanf("%d",&a);
   a= a%10;
   b= a/10;
   if( (a+b)%2 ==0)
   	printf("so chan");
   	
    else
   	printf("sole");
   	
   	int n;
   	scanf("%d", &n);
   	
   	(n%2 ==0)? printf("chan") : printf ("le");
   	*/
   	/*float a,b;
   	char c;
   	scanf("%f%f",&a,&b);
   	fflush(stdin);
   	scanf("%c",&c);
   	if(c== '+') printf("%.2f + %2.f = %.2f", a, b, a+b);
   	if(c== '-') printf("%.2f - %.2f = %.2f", a, b, a-b);
   	if(c== '*') printf ("%.2f * %.2f = %.2f", a, b, a*b);
   	if(c== '/') printf( if(b= 0) printf("khonglmdc");
	   else printf ("%.2f / %.2f", a,b,a/b);); */
	  /* int a;
	   printf("nhap so");
	   int tong =0;
	   scanf ("%d", &a);
	   for (int i = 0; i <= a; i++) {
	   	tong+=i;
	   }
   	printf ("%d", tong); */
   	
   	printf ("%c", 179);
   	
   
   return 0;
   
}

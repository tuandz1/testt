#include <stdio.h>
#define M 10
int main () {
	 printf ("Hello world\n");
	 
	 float number = 1.5;
	 const int numbers = 1;
	 printf("%d\n", numbers);
	 
	 printf("10\n"); // printf("%d\n", M);
	 double number2 = 1.6;
	 char c = 'c';
	 int gen = 10;
	 
	 printf(" chung ta la thanh vien gen %d %d, hooray", numbers, gen);
	 printf("JS bat dau bang %c\n",c);
	 printf("1 + 0.5 = %2.1f\n",number);
	 
	 int age = 18;
	 
	 
	 /*float tall, date, month, year;
	 scanf("%f",&tall);
	 printf("toi cao %.1f", tall);
	 scanf("%f%f%f", &date,&month,&year);
	 printf("toi sinh ngay %.0f thang %.0f nam %.0f", date,month,year );
	 */
	 /*int a,b;
	 printf("nhap hai so nguyen a va b : ");
	 scanf("%d%d",&a ,&b);
	 int c = a;
		a = b;
		b = c;
	 	printf("sau khi doi so la %d va %d ", a, b);
	 	
	/* int a,b; 
	printf("") */
	 
	 

    return 0;
}
